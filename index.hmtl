<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<title>🎄 Merry Christmas 🎄</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
html, body {
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: radial-gradient(circle at bottom, #020024, #090979, #000);
    height: 100%;
    font-family: 'Segoe UI', sans-serif;
}

/* ===== XOAY MÀN HÌNH 360 ===== */
#rotate {
    position: absolute;
    width: 100%;
    height: 100%;
    animation: spin 20s linear infinite;
}
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

/* ===== VŨ TRỤ ===== */
.star {
    position: absolute;
    width: 2px;
    height: 2px;
    background: white;
    border-radius: 50%;
    animation: twinkle 2s infinite alternate;
}
@keyframes twinkle {
    from { opacity: 0.3; }
    to { opacity: 1; }
}

/* ===== TUYẾT ===== */
.snow {
    position: absolute;
    color: white;
    font-size: 10px;
    animation: snowFall linear infinite;
}
@keyframes snowFall {
    from { transform: translateY(-10px); }
    to { transform: translateY(110vh); }
}

/* ===== CÂY THÔNG ===== */
#tree {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
}

.leaf {
    width: 0;
    height: 0;
    border-left: 10px solid transparent;
    border-right: 10px solid transparent;
    border-bottom: 20px solid;
    margin: auto;
    animation: colorShift 3s infinite linear, spiral 3s ease-out forwards;
}

@keyframes colorShift {
    0% { border-bottom-color: red; }
    33% { border-bottom-color: lime; }
    66% { border-bottom-color: cyan; }
    100% { border-bottom-color: red; }
}

@keyframes spiral {
    from { transform: rotate(0deg) translateX(0); }
    to { transform: rotate(720deg) translateX(20px); }
}

/* ===== LỜI CHÚC ===== */
#message {
    position: absolute;
    top: 10%;
    width: 100%;
    text-align: center;
    font-size: 22px;
    font-weight: bold;
}

.line {
    opacity: 0;
    animation: showLine 1s forwards;
}
@keyframes showLine {
    to { opacity: 1; }
}
</style>
</head>

<body>

<div id="rotate">

    <!-- LỜI CHÚC -->
    <div id="message"></div>

    <!-- CÂY THÔNG -->
    <div id="tree"></div>

</div>

<!-- NHẠC NOEL -->
<audio id="music" loop>
    <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3">
</audio>

<script>
/* ===== CLICK ĐỂ PHÁT NHẠC ===== */
document.body.addEventListener("click", () => {
    document.getElementById("music").play();
});

/* ===== SAO VŨ TRỤ ===== */
for (let i = 0; i < 150; i++) {
    let s = document.createElement("div");
    s.className = "star";
    s.style.left = Math.random() * 100 + "%";
    s.style.top = Math.random() * 100 + "%";
    s.style.animationDuration = (1 + Math.random() * 3) + "s";
    document.body.appendChild(s);
}

/* ===== TUYẾT RƠI ===== */
for (let i = 0; i < 80; i++) {
    let snow = document.createElement("div");
    snow.className = "snow";
    snow.innerHTML = "❄";
    snow.style.left = Math.random() * 100 + "%";
    snow.style.animationDuration = (5 + Math.random() * 5) + "s";
    snow.style.fontSize = (8 + Math.random() * 15) + "px";
    document.body.appendChild(snow);
}

/* ===== CÂY THÔNG XOÁY TỪ DƯỚI LÊN ===== */
const tree = document.getElementById("tree");
let
